cmake_minimum_required(VERSION 3.10)
project(myproject)

set(CMAKE_CXX_STANDARD 10)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# add vcpkg.cmake
set(CMAKE_TOOLCHAIN_FILE ${CMAKE_SOURCE_DIR}/vcpkg/scripts/buildsystems/vcpkg.cmake)

# add opencv external library
find_package(OpenCV REQUIRED)

# GaussianBlur
add_executable(GaussianBlur gaussianBlur.cpp)
target_link_libraries(GaussianBlur PRIVATE ${OpenCV_LIBS})
# add opencv external headers
target_include_directories(GaussianBlur PRIVATE /vcpkg/installed/x64-linux/include)
